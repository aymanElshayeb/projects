This project is a small project to illustrate the main features of angluarjs

The project is a login page for the user

it displays the login screen if the user inserts the correct user name/password it can access 
success page otherwise it asked to insert the correct one.

The name should not be empty

the usename and password are hard coded for simplisty

